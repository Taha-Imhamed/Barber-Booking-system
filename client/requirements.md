## Packages
date-fns | Formatting dates and times beautifully
zustand | Simple global state management for the UI

## Notes
- Wouter is used for routing.
- The UI uses a custom dark theme with Amber/Gold accents to fit a luxury barbershop aesthetic.
- The Wall Display page (`/display`) plays an audio notification when new pending appointments arrive.
- Make sure to seed `branches` and `services` in the database so the booking form has options.
